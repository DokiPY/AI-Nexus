# AI Agent 编排平台 - 系统简介

## 系统概述

这是一个**企业级 AI Agent 编排平台**，通过集成 N8N 工作流引擎，为企业提供灵活的 AI 智能体服务。

简单来说：
- **平台作用**：为企业员工提供多种 AI 智能助手（客服、文案、数据分析等）
- **N8N 作用**：负责 AI 的实际执行逻辑（调用大模型、处理数据、对接第三方服务）
- **两者关系**：本平台是"前台管理系统"，N8N 是"后台执行引擎"

## 核心价值

### 1. 多租户企业管理
- 支持多个公司独立使用
- 每个公司有自己的员工和权限体系
- 管理员可以灵活分配员工使用哪些 AI 工作流

### 2. 工作流编排能力
- 管理员在 N8N 中设计 AI 工作流（比如：客服机器人、文案生成器）
- 在本平台配置工作流信息（名称、图标、描述、N8N Webhook 地址）
- 员工通过统一界面使用各种 AI 能力，无需了解底层技术

### 3. 完整的会话管理
- 类似 ChatGPT 的对话体验
- 支持多会话管理
- 聊天记录持久化存储
- 软删除机制，数据可恢复

## 什么是 N8N？

**N8N** 是一个开源的工作流自动化工具，类似于 Zapier 或 Make（Integromat）。

### N8N 的特点
- **可视化编排**：通过拖拽节点设计工作流
- **丰富的集成**：支持 400+ 种服务（OpenAI、数据库、API、邮件等）
- **灵活的逻辑**：支持条件判断、循环、数据转换
- **Webhook 触发**：可以通过 HTTP 请求触发工作流

### N8N 工作流示例

**客服机器人工作流：**
```
用户消息 → 查询知识库 → 调用 OpenAI → 格式化回复 → 返回结果
```

**文案生成器工作流：**
```
用户需求 → 提取关键词 → 调用 GPT-4 → 优化文案 → 保存到数据库 → 返回
```

**数据分析助手工作流：**
```
用户问题 → 查询数据库 → 生成图表 → 调用 AI 分析 → 返回报告
```

## 系统与 N8N 的集成方式

### 工作流程

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│  前端界面    │ ───> │  本平台后端   │ ───> │  N8N 引擎   │
│  (用户输入)  │      │  (权限/会话)  │      │  (AI处理)   │
└─────────────┘      └──────────────┘      └─────────────┘
       ↑                     ↑                      │
       │                     │                      │
       └─────────────────────┴──────────────────────┘
                    返回 AI 回复并保存
```

### 集成细节

1. **配置阶段**
   - 管理员在 N8N 中创建工作流
   - N8N 生成 Webhook URL（如：`https://n8n.example.com/webhook/abc123`）
   - 管理员在本平台录入工作流信息和 Webhook URL

2. **运行阶段**
   - 用户在前端发送消息
   - 后端检查权限，创建/获取会话
   - 后端调用 N8N Webhook，传递：
     - 用户消息内容
     - 用户信息（ID、姓名、公司等）
     - 会话上下文（可选）
   - N8N 执行工作流，返回 AI 回复
   - 后端保存对话记录，返回给前端

3. **数据传递示例**
```json
// 发送给 N8N
{
  "message": "帮我写一篇关于AI的文章",
  "user": {
    "id": 123,
    "username": "张三",
    "company_id": 1
  }
}

// N8N 返回
{
  "output": "这是生成的文章内容..."
}
```

## 为什么要这样设计？

### 分离关注点
- **本平台**：专注于企业管理、权限控制、用户体验
- **N8N**：专注于 AI 逻辑编排、第三方集成、复杂流程

### 灵活性
- 不修改代码，只需在 N8N 中调整工作流
- 可以快速上线新的 AI 能力
- 支持不同的大模型（OpenAI、Claude、国产模型等）

### 可扩展性
- 一个平台可以对接多个 N8N 实例
- 不同工作流可以使用不同的技术栈
- 便于团队协作（前端、后端、AI 工程师分工明确）

## 典型使用场景

### 场景 1：客服机器人
- **员工角色**：客服人员
- **工作流**：智能客服助手
- **N8N 逻辑**：查询知识库 → 调用 GPT → 返回答案
- **价值**：提高客服效率，标准化回复

### 场景 2：营销文案生成
- **员工角色**：市场部
- **工作流**：文案生成器
- **N8N 逻辑**：分析产品信息 → 调用 AI → 生成多版本文案
- **价值**：快速产出高质量营销内容

### 场景 3：数据分析助手
- **员工角色**：业务分析师
- **工作流**：数据查询分析
- **N8N 逻辑**：SQL 查询 → 数据可视化 → AI 解读
- **价值**：降低数据分析门槛

## 技术优势

- ✅ **开箱即用**：员工无需学习 N8N，通过统一界面使用
- ✅ **权限可控**：管理员精细控制谁能用哪些 AI 能力
- ✅ **数据留存**：所有对话记录保存，便于审计和优化
- ✅ **成本透明**：记录每次调用的响应时间，便于成本分析
- ✅ **快速迭代**：修改 AI 逻辑只需调整 N8N 工作流，无需改代码

## 总结

这个平台的核心理念是：**让 AI 能力像企业应用一样易于管理和使用**。

- 对于**管理员**：提供完整的企业管理、权限分配、工作流配置能力
- 对于**员工**：提供简洁的聊天界面，像使用 ChatGPT 一样使用企业 AI
- 对于**技术团队**：通过 N8N 灵活编排 AI 逻辑，快速响应业务需求

通过本平台 + N8N 的组合，企业可以快速构建自己的 AI 智能体矩阵，提升整体工作效率。
